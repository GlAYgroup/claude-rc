# user config (contains folder paths and access token)
config.sh

# runtime artifacts
logs/
*.log
*.sock
*.crash.log

# os / editor
.DS_Store
.idea/
.vscode/

# native app build output (rebuild via native/build.sh)
native/build/
